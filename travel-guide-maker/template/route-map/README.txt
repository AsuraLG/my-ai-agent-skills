在此放置路线图 PNG 文件（建议命名为 route-map.png），并在 guide.config.json 中的 routeMapPath 字段指向它。

推荐尺寸：宽高比接近 2:3（竖向），例如 800×1200px 以上。
支持格式：PNG（推荐）或 JPG。

获取方式：
- 在高德地图 / 百度地图 / Google Maps 中规划路线，截图保存
- 也可以用任何图片工具手绘路线示意图
